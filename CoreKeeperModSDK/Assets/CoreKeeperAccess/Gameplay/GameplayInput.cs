namespace CoreKeeperAccess.Gameplay
{
    // [COQUILLE — a remplir] Moteur de keymaps gameplay : lecture brute des
    // sticks/boutons (Rewired), multi-appui (double/triple), boutons modificateurs
    // (facon Maj/Ctrl), et couches de keymaps (modes a11y). Socle des commandes
    // gameplay (infos vitales, bascule de modes, canne). Cf. PlayerInput cote jeu.
    internal static class GameplayInput
    {
    }
}
