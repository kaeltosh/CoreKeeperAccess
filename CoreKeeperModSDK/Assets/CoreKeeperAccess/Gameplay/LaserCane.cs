namespace CoreKeeperAccess.Gameplay
{
    // [COQUILLE — a remplir] Canne laser 360° (stick droit, visee native aimDirection) :
    // un rayon part du joueur, avance tuile par tuile dans l'espace ouvert, rapporte
    // le contenu des cases eclairees (son natif spatialise + TTS), bute sur le premier
    // mur (revele sa face). Perception dynamique pour le mode combat/exploration.
    // ⚠ La dualite onde/particule (balayage vs sonde ponctuelle) n'est PAS tranchee.
    internal static class LaserCane
    {
    }
}
