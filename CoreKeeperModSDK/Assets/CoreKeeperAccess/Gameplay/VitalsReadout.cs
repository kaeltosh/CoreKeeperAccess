namespace CoreKeeperAccess.Gameplay
{
    // [COQUILLE — a remplir] Lecture des etats vitaux a la demande : vie
    // (currentHealth / GetMaxHealth), faim (hungerComponent), mana (ManaCD),
    // barriere magique (MagicBarrierCD). Annonce TTS sur commande (cf. GameplayInput).
    internal static class VitalsReadout
    {
    }
}
